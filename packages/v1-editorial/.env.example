# Used at runtime (Vite) and during `pnpm build` for the prerender step (tsx).
# No trailing slash. Example on static hosting under /v1: https://example.com/v1
VITE_CANONICAL_BASE_URL=http://127.0.0.1:5173
